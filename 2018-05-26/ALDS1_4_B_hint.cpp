#include<cstdio>

// Aにkeyがあるなら1，ないなら0を返す
int binarySearch(int A[],int n,int key){
  int left=0;
  int right = n;
  int mid;
  while(left<right){
    // TODO
    // keyがmidにある場合
    // keyが後半にある場合
    // keyが前半にある場合
  }
  return 0;
}

int main(){
  int n,q;
  scanf("%d",&n);
  int S[n];
  for(int i=0;i<n;i++){
    scanf("%d",&S[i]);
  }
  scanf("%d",&q);
  int t;
  int sum=0;
  for(int i=0;i<q;i++){
    scanf("%d",&t);
    // TODO
  }
  printf("%d\n",sum);

}
